PICPLANR STRIPE CHECKOUT DIAGNOSTIC

Replace only:

api/billing/checkout.js

This file does not change plans, prices or billing behaviour. It makes PicPlanr
show the exact safe reason checkout is failing, such as:

• Missing Vercel secret key
• Missing Stripe Price ID
• Test/live mode mismatch
• Missing Supabase subscription table
• Invalid customer or price
• Stripe account setup restriction

After replacing the file:

1. Commit it.
2. Wait for the Production deployment.
3. Click Choose Starter once.
4. Send a screenshot of the new message.

Do not change Stripe, Supabase or Vercel again until the new message identifies
the exact failure.
