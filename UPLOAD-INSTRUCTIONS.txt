PICPLANR VERSION 36.3 — RELIABLE GROUPING

Upload both files to the v35-clean-rebuild branch:

1. index.html
   Replace the existing top-level index.html.

2. api/picplanr.js
   Open the api folder and replace picplanr.js.

What this fixes:
- Images are grouped in batches of 8 instead of one large request.
- One failed grouping batch no longer stops the whole folder.
- Ungrouped images are recovered as safe single-post ideas.
- Error messages now show the actual server problem.
- Natural Story caption labels are consistent in the API prompt.

After deployment:
1. Confirm the sidebar says Version 36.3.
2. Upload the same folder again.
3. Run Analyse and create post groups.
