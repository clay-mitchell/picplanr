PICPLANR VERSION 36.4 — INSTAGRAM RETURN FLOW

Upload both files to the v35-clean-rebuild branch:

1. index.html
   Replace the top-level index.html.

2. api/oauth/instagram/callback.js
   Open api > oauth > instagram and replace callback.js.

What changes:
- Instagram opens in a connection popup.
- After approval, the popup returns to PicPlanr and closes automatically.
- PicPlanr refreshes the connected Instagram status.
- PicPlanr opens Account Review and starts the live Instagram analysis.
- If the popup is blocked, the normal full-page redirect still works.

Important:
META_REDIRECT_URI in Vercel must still exactly match:
https://YOUR-PICPLANR-DOMAIN/api/oauth/instagram/callback
