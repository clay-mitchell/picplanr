import {parseCookies,cookie} from '../../_lib/http.js';import {db} from '../../_lib/google-calendar.js';
export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed.'});const id=parseCookies(req).pp_google_connection;try{if(id)await db(`google_calendar_connections?id=eq.${encodeURIComponent(id)}`,{method:'PATCH',prefer:'return=minimal',body:{status:'disconnected',updated_at:new Date().toISOString()}});res.setHeader('Set-Cookie',cookie('pp_google_connection','',{maxAge:0}));return res.status(200).json({disconnected:true})}catch(e){return res.status(500).json({error:'Google Calendar could not be disconnected.'})}}
