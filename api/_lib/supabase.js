
PICPLANR SUPABASE URL FIX

Upload this file to:

api > _lib > supabase.js

Replace the existing supabase.js.

Also check Vercel:
SUPABASE_URL must contain only the project URL, for example:

https://abcdefghijk.supabase.co

Do not include:
/rest/v1
/api
npm install commands
quotation marks
spaces before or after the URL

After replacing the file and correcting the variable, redeploy and start a new
Instagram connection.
